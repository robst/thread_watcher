require 'spec_helper'

describe ThreadWatcher do
  it 'has a version number' do
    expect(ThreadWatcher::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
